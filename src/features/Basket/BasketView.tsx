const BasketView = () => {
  /* useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await getCart()
      } catch (error) {
        console.error(error);
      }
    }
  }, []) */
  return;
};

export default BasketView;
